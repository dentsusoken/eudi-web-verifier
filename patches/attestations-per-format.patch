--- src/app/core/constants/attestations-per-format.ts	2026-03-06 18:15:57
+++ src/app/core/constants/attestations-per-format_local.ts	2026-03-14 00:00:00
@@ -1,5 +1,5 @@
 import {Attestation, MsoMdocAttestation, SdJwtVcAttestation} from "@core/models/attestation/Attestations";
-import {EHIC_ATTESTATION, EHIC_ATTESTATION_DC4EU, MDL_ATTESTATION, PDA1_ATTESTATION, PHOTO_ID_ATTESTATION, PID_ATTESTATION, LEARNING_CREDENTIAL_ATTESTATION} from "@core/constants/attestation-definitions";
+import {EHIC_ATTESTATION, EHIC_ATTESTATION_DC4EU, MDL_ATTESTATION, PDA1_ATTESTATION, PHOTO_ID_ATTESTATION, PID_ATTESTATION, LEARNING_CREDENTIAL_ATTESTATION, ACADEMIC_CREDIT_2024_FALL_ATTESTATION, ACADEMIC_CREDIT_2025_SPRING_ATTESTATION, UNIVERSITY_STUDENT_ATTESTATION} from "@core/constants/attestation-definitions";
 import {AttestationFormat} from "@core/models/attestation/AttestationFormat";
 import {AttestationType} from "@core/models/attestation/AttestationType";
 import {DataElement} from "@core/models/attestation/AttestationDefinition";
@@ -87,6 +87,30 @@
   claimQuery: (attribute: DataElement) => { return { path: sdJwtVcAttributeClaimQuery(attribute, AttestationType.LEARNING_CREDENTIAL) } }
 }

+/*---- ACADEMIC CREDIT 2024 FALL INSTANCES PER FORMAT ----*/
+export const ACADEMIC_CREDIT_2024_FALL_SD_JWT_VC: SdJwtVcAttestation = {
+  format: AttestationFormat.SD_JWT_VC,
+  attestationDef: ACADEMIC_CREDIT_2024_FALL_ATTESTATION,
+  vct: "urn:com:dentsusoken:academic_credit:2024:03",
+  claimQuery: (attribute: DataElement) => { return { path: [attribute.identifier] } }
+}
+
+/*---- ACADEMIC CREDIT 2025 SPRING INSTANCES PER FORMAT ----*/
+export const ACADEMIC_CREDIT_2025_SPRING_SD_JWT_VC: SdJwtVcAttestation = {
+  format: AttestationFormat.SD_JWT_VC,
+  attestationDef: ACADEMIC_CREDIT_2025_SPRING_ATTESTATION,
+  vct: "urn:com:dentsusoken:academic_credit:2025:01",
+  claimQuery: (attribute: DataElement) => { return { path: [attribute.identifier] } }
+}
+
+/*---- UNIVERSITY STUDENT INSTANCES PER FORMAT ----*/
+export const UNIVERSITY_STUDENT_SD_JWT_VC: SdJwtVcAttestation = {
+  format: AttestationFormat.SD_JWT_VC,
+  attestationDef: UNIVERSITY_STUDENT_ATTESTATION,
+  vct: "https://jp.ac.example-university/vct/student-id",
+  claimQuery: (attribute: DataElement) => { return { path: [attribute.identifier] } }
+}
+
 function resolveAttribute(attribute: DataElement, attestationType: AttestationType): string {
   let resolvedAttribute = attribute.identifier
   if (attestationType === AttestationType.PID) {
@@ -141,7 +165,7 @@

 export const ATTESTATIONS_BY_FORMAT: { [id: string]: Attestation[] } = {
   "mso_mdoc": [PID_MSO_MDOC, MDL_MSO_MDOC, PHOTO_ID_MSO_MDOC, EHIC_MSO_MDOC, PDA1_MSO_MDOC],
-  "dc+sd-jwt": [PID_SD_JWT_VC, EHIC_SD_JWT_VC, PDA1_SD_JWT_VC, EHIC_SD_JWT_VC_DC4EU, LEARNING_CREDENTIAL_SD_JWT_VC]
+  "dc+sd-jwt": [PID_SD_JWT_VC, EHIC_SD_JWT_VC, PDA1_SD_JWT_VC, EHIC_SD_JWT_VC_DC4EU, LEARNING_CREDENTIAL_SD_JWT_VC, ACADEMIC_CREDIT_2024_FALL_SD_JWT_VC, ACADEMIC_CREDIT_2025_SPRING_SD_JWT_VC, UNIVERSITY_STUDENT_SD_JWT_VC]
 }

 export const getAttestationByFormatAndType =
